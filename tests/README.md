# tests/

Per-stage correctness tests. No ticket in `../docs/dev/GWB_PIPELINE_REFACTOR_PLAN.md` counts as
done without a test here that verifies the stage produces the products it's meant for —
this is a standing, non-negotiable rule for this rebuild, not optional scaffolding.
