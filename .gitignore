.DS_Store
__pycache__/
.vscode/
*.bak
*.bak2
*.bak3
*.log
*.log.*

# Output directories (generated by workflows, not source code)
archive/
casa_out/
diagnostics_out/
/tmp/
/tmp_*/

# Work subdirectories (experimental/temporary work)
work/moon_trajectory/
work/stripe_diag/
work/stripe_recheck/
work/tests/
work/uvfits_ms_audit/

# CASA temporary lattice/table artifacts
TempLattice*/
*.tablelock

# Local/generated artifacts not ready for versioning (legacy GSB tree)
legacy_gsb_40_014/experimental/*.png
legacy_gsb_40_014/experimental/*.gif
share/*.png
handy_commands.txt
legacy_gsb_40_014/preprocess_ugmrt.ipynb
ugmrt_fits_metadata_queries.ipynb


# pytest
.pytest_cache/

# Python virtualenv (rebuilt 2026-09-23 with stdlib venv, which -- unlike the
# original virtualenv-created one -- doesn't drop its own internal .gitignore).
# Root-anchored (/gmrt/, not gmrt/) so this doesn't also swallow
# src/instruments/gmrt/ -- caught doing exactly that before the first commit
# of that directory.
/gmrt/
