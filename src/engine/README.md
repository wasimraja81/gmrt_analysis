# src/engine/

Core scientific logic for the GWB pipeline: bandpass solve, flagging, clustering,
secondary calibration, split, imaging. Written fresh — informed by reading the archived
GSB engine (`../../legacy_gsb_40_014/src/modules/ugmrt_query.py` and siblings) for
behavioral intent, never by copying files or names forward from it.

Empty until the first calibration-stage ticket lands — see
`../../docs/dev/GWB_PIPELINE_REFACTOR_PLAN.md`.
